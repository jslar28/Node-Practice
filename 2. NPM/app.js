var express = require('express');

console.log("To install a dependency globally, you can type: npm install -g <dependency name>.");
console.log("To install a dependency locally, you can type: npm install <dependency name>");
console.log("Running npm install will install express locally.");